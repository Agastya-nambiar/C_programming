#include <stdio.h>
int main() {
    int unit;
    printf("Number of units: ");
    scanf("%d", &unit);
    
     if (unit<=50) {
        unit=unit*.5
        printf("The maximum number is: %d\n", num1);
    } else if (num2 > num1) {
        printf("The maximum number is: %d\n", num2);
    } else {
        printf("Both numbers are equal.\n");
    }
    return 0;
}